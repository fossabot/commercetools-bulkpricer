package com.commercetools.bulkpricer.helpers;

//import com.koloboke.compile.KolobokeMap;
//import com.koloboke.compile.hash.algo.openaddressing.QuadraticHashing;
//import com.koloboke.compile.mutability.Updatable;
//
//@KolobokeMap
//@Updatable
//@QuadraticHashing
public abstract class PriceMap<K> {

/*
  static <K> PriceMap<K> withExpectedSize(int expectedSize) {
    return new KolobokePriceMap<K>(
      // low growth factor to optimize for memory footprint and not load performance
      HashConfig.getDefault().withGrowthFactor(1.1), expectedSize);
  }

  abstract void justPut(K key, int value);

  abstract int getInt(K key);

  abstract void clear();

  abstract int size();
*/
}
